import Typography from "@mui/material/Typography";

function PublicReportsTabContent(): JSX.Element {
  return <Typography variant="h4">public report listing</Typography>;
}

export default PublicReportsTabContent;
